name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  # CI identifier (all jobs load .env, but ENV=CI enables CI-specific behavior)
  ENV: CI

jobs:
  # Unit Tests and Linting (Parallel)
  # TEMPORARILY DISABLED - focusing on e2e-collab-docker
  unit-backend:
    if: false
    runs-on: ubuntu-latest
    env:
      JWT_SECRET_KEY: test_secret_key_for_ci_environment_32_chars_minimum
      COPILOT_PROVIDER: mock

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_aris
          POSTGRES_HOST_AUTH_METHOD: trust
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Clone RSM repository
      run: |
        cd ..
        git clone --recurse-submodules https://github.com/aris-pub/rsm.git
        echo "Cloned RSM repository to ../rsm"

    - name: Install uv
      uses: astral-sh/setup-uv@v5
      with:
        enable-cache: true

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13'

    - name: Install RSM dependencies
      run: |
        cd ../rsm
        uv sync

    - name: Install backend dependencies
      run: |
        cd backend
        uv sync --all-groups

    - name: Create backend .env file
      run: cp backend/.env.example backend/.env

    - name: Debug - Test PostgreSQL connection
      run: |
        echo "Testing PostgreSQL connection..."
        cd backend
        uv run python -c "
        import asyncio
        import asyncpg

        async def test():
            # Test connection to test_aris
            try:
                conn = await asyncpg.connect(
                    host='localhost',
                    port=5432,
                    user='postgres',
                    password='postgres',
                    database='test_aris'
                )
                print('✅ Connection to test_aris successful!')
                version = await conn.fetchval('SELECT version()')
                print(f'PostgreSQL version: {version}')
                await conn.close()
            except Exception as e:
                print(f'❌ Connection to test_aris failed: {e}')
                raise

            # Test connection to postgres admin database
            try:
                conn = await asyncpg.connect(
                    host='localhost',
                    port=5432,
                    user='postgres',
                    password='postgres',
                    database='postgres'
                )
                print('✅ Connection to postgres admin db successful!')
                await conn.close()
            except Exception as e:
                print(f'❌ Connection to postgres admin db failed: {e}')
                raise

        asyncio.run(test())
        "

    - name: Run linting
      run: |
        cd backend
        uv run ruff check

    - name: Run type checking
      run: |
        cd backend
        uv run mypy aris/

    - name: Run tests
      run: |
        cd backend
        uv run pytest -n4 --dist=loadscope --tb=short

  unit-frontend:
    if: false  # Temporarily disabled - focusing on e2e-collab-docker
    runs-on: ubuntu-latest
    env:
      BACKEND_PORT: 8000
      VITE_API_BASE_URL: http://localhost:8000

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '23'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install frontend dependencies
      run: |
        cd frontend
        npm ci

    - name: Run linting
      run: |
        cd frontend
        npm run lint

    - name: Run tests
      run: |
        cd frontend
        npm run test:run

  unit-site:
    if: false  # Temporarily disabled - focusing on e2e-collab-docker
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: site

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Clone RSM repository
      run: |
        cd ..
        git clone --recurse-submodules https://github.com/aris-pub/rsm.git
        echo "Cloned RSM repository to ../rsm"

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '23'
        cache: 'npm'
        cache-dependency-path: site/package-lock.json

    - name: Install dependencies
      run: npm ci

    - name: Cache vitest
      uses: actions/cache@v5
      with:
        path: site/node_modules/.vitest
        key: vitest-site-${{ runner.os }}-${{ hashFiles('site/package-lock.json') }}

    - name: Check for missing packages
      run: npm ls --depth=0

    - name: Run linting
      run: npm run lint

    - name: Run unit tests
      run: npm run test:run

  # Site E2E Tests (All Browsers)
  e2e-site:
    if: false  # Temporarily disabled - focusing on e2e-collab-docker
    needs: [unit-backend, unit-frontend, unit-site]
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox]
        device: [desktop, mobile]
    runs-on: ubuntu-latest
    env:
      ENV: CI
      BACKEND_PORT: 8000
      FRONTEND_PORT: 5173
      SITE_PORT: 3000
      STORYBOOK_PORT: 6006
      DB_PORT: 5432
      DB_NAME: aris
      TEST_DB_NAME: aris_test
      VITE_API_BASE_URL: http://localhost:8000
      FRONTEND_URL: http://localhost:5173
      NUXT_BACKEND_URL: http://localhost:8000
      TEST_USER_EMAIL: testuser@aris.pub
      TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD || 'testpassword123' }}

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_aris
          POSTGRES_HOST_AUTH_METHOD: trust
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 3
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Clone RSM repository
      run: |
        cd ..
        git clone --recurse-submodules https://github.com/aris-pub/rsm.git
        echo "Cloned RSM repository to ../rsm"

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '23'
        cache: 'npm'
        cache-dependency-path: site/package-lock.json

    - name: Install dependencies
      working-directory: site
      run: npm ci

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm ci

    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        version: "latest"
        enable-cache: true

    - name: Set up Python
      run: uv python install 3.13

    - name: Wait for PostgreSQL to be ready
      run: |
        timeout 30 bash -c 'until pg_isready -h localhost -p 5432 -U postgres; do sleep 1; done'
        echo "PostgreSQL is ready!"

    - name: Create virtual environment and sync dependencies
      working-directory: backend
      run: |
        uv venv
        uv sync --group test --group dev
        # Force reinstall RSM from local clone to override the locked git version
        uv pip install --reinstall --no-deps -e ../../rsm

    - name: Run database migrations
      working-directory: backend
      run: uv run alembic upgrade head

    - name: Cache Playwright browsers
      uses: actions/cache@v5
      id: playwright-cache
      with:
        path: ~/.cache/ms-playwright
        key: playwright-browsers-site-${{ matrix.browser }}-${{ matrix.device }}

    - name: Install Playwright browsers
      working-directory: site
      if: steps.playwright-cache.outputs.cache-hit != 'true'
      run: npx playwright install ${{ matrix.browser }} --with-deps

    - name: Start backend, frontend, site, and run E2E tests
      run: |
        # Start backend in background
        echo "Starting backend for site E2E tests..."
        cd backend
        uv run uvicorn main:app --host 0.0.0.0 --port 8000 &
        BACKEND_PID=$!
        echo "Backend started with PID: $BACKEND_PID"
        
        # Wait for backend to be ready
        sleep 8
        for i in {1..5}; do
          if curl -f http://localhost:8000/health; then
            echo "Backend ready for E2E tests"
            break
          else
            echo "Waiting for backend... ($i/5)"
            if [ $i -eq 5 ]; then
              echo "Backend failed to start"
              kill $BACKEND_PID 2>/dev/null || true
              kill $MULTIPLAYER_PID 2>/dev/null || true
              exit 1
            fi
            sleep 2
          fi
        done
        
        # Start frontend in background
        echo "Starting frontend for site E2E tests..."
        cd ../frontend
        npm run dev &
        FRONTEND_PID=$!
        echo "Frontend started with PID: $FRONTEND_PID"
        
        # Wait for frontend to be ready
        sleep 15
        for i in {1..5}; do
          if curl -f http://localhost:5173; then
            echo "Frontend ready for E2E tests"
            break
          else
            echo "Waiting for frontend... ($i/5)"
            if [ $i -eq 5 ]; then
              echo "Frontend failed to start"
              kill $FRONTEND_PID 2>/dev/null || true
              kill $BACKEND_PID 2>/dev/null || true
              exit 1
            fi
            sleep 3
          fi
        done
        
        # Start site in background
        echo "Starting site for ${{ matrix.browser }} ${{ matrix.device }} E2E tests..."
        cd ../site
        npm run dev &
        SITE_PID=$!
        echo "Site started with PID: $SITE_PID"
        
        # Wait for site to be ready
        sleep 10
        for i in {1..5}; do
          if curl -f http://localhost:3000; then
            echo "Site ready for E2E tests"
            break
          else
            echo "Waiting for site... ($i/5)"
            if [ $i -eq 5 ]; then
              echo "Site failed to start"
              kill $SITE_PID 2>/dev/null || true
              kill $FRONTEND_PID 2>/dev/null || true
              kill $BACKEND_PID 2>/dev/null || true
              exit 1
            fi
            sleep 3
          fi
        done
        
        # Run site E2E tests for specific browser and device
        if [ "${{ matrix.device }}" = "mobile" ]; then
          if [ "${{ matrix.browser }}" = "firefox" ]; then
            npx playwright test --project="Mobile Firefox" --reporter=github
          else
            npx playwright test --project="Mobile Chrome" --reporter=github
          fi
        else
          if [ "${{ matrix.browser }}" = "firefox" ]; then
            npx playwright test --project="firefox" --reporter=github
          else
            npx playwright test --project="chromium" --reporter=github
          fi
        fi
        TEST_EXIT_CODE=$?
        
        # Cleanup
        echo "Cleaning up processes..."
        kill $SITE_PID 2>/dev/null || true
        kill $FRONTEND_PID 2>/dev/null || true
        kill $BACKEND_PID 2>/dev/null || true
        exit $TEST_EXIT_CODE

  # Desktop E2E Tests (Chrome)
  e2e-desktop-chrome:
    if: false  # Temporarily disabled to speed up CI testing
    # needs: [unit-backend, unit-frontend, unit-site]  # TEMPORARILY DISABLED
    strategy:
      fail-fast: false
      matrix:
        test-type: [demo, auth]
    runs-on: ubuntu-latest
    env:
      TEST_USER_EMAIL: testuser@aris.pub
      TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
      VITE_LOG_LEVEL: WARN
      VITE_MULTIPLAYER_URL: ws://localhost:1234
      COPILOT_PROVIDER: mock

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_aris
          POSTGRES_HOST_AUTH_METHOD: trust
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 3
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Clone RSM repository
      run: |
        cd ..
        git clone --recurse-submodules https://github.com/aris-pub/rsm.git
        echo "Cloned RSM repository to ../rsm"

    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        version: "latest"
        enable-cache: true

    - name: Set up Python
      run: uv python install 3.13

    - name: Wait for PostgreSQL to be ready
      run: |
        timeout 30 bash -c 'until pg_isready -h localhost -p 5432 -U postgres; do sleep 1; done'
        echo "PostgreSQL is ready!"

    - name: Create virtual environment and sync dependencies
      working-directory: backend
      run: |
        uv venv
        uv sync --group test --group dev
        uv pip install -e ../../rsm

    - name: Run database migrations and setup test user (with debugging)
      working-directory: backend
      run: |
        echo "🔍 [DEBUG] Starting database setup for test-type: ${{ matrix.test-type }}"
        echo "🔍 [DEBUG] Environment variables:"
        echo "  - TEST_USER_EMAIL: ${TEST_USER_EMAIL:-UNSET}"
        echo "  - TEST_USER_PASSWORD length: ${#TEST_USER_PASSWORD}"
        echo "  - DB_URL_LOCAL: ${DB_URL_LOCAL:-UNSET}"
        echo "  - PostgreSQL connection test:"

        # Test database connectivity
        if pg_isready -h localhost -p 5432 -U postgres; then
          echo "✅ PostgreSQL connection successful"
        else
          echo "❌ PostgreSQL connection failed"
          exit 1
        fi

        # Run migrations
        echo "🔄 Running database migrations..."
        uv run alembic upgrade head
        MIGRATION_EXIT_CODE=$?
        echo "📊 Migration exit code: $MIGRATION_EXIT_CODE"

        # Setup test user for enabled auth mode
        if [ "${{ matrix.test-type }}" = "auth" ]; then
          echo "🔑 Auth tests - creating test user..."
          echo "📧 Creating test user: ${TEST_USER_EMAIL}"
          uv run python scripts/reset_test_user.py
          RESET_USER_EXIT_CODE=$?
          echo "📊 Reset test user exit code: $RESET_USER_EXIT_CODE"

          if [ $RESET_USER_EXIT_CODE -eq 0 ]; then
            echo "✅ Test user creation/reset completed successfully"
          else
            echo "❌ Test user creation/reset FAILED with exit code: $RESET_USER_EXIT_CODE"
            exit $RESET_USER_EXIT_CODE
          fi
        else
          echo "🔓 Demo tests - skipping test user creation"
        fi

        echo "✅ [DEBUG] Database setup completed for ${{ matrix.test-type }} tests"

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '23'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm ci

    - name: Install multiplayer server dependencies
      working-directory: multi-player
      run: npm ci

    - name: Cache Playwright browsers
      uses: actions/cache@v5
      id: playwright-cache
      with:
        path: ~/.cache/ms-playwright
        key: playwright-browsers-${{ runner.os }}-chromium

    - name: Install Playwright browsers
      working-directory: frontend
      if: steps.playwright-cache.outputs.cache-hit != 'true'
      run: npx playwright install chromium --with-deps

    - name: Start backend and frontend, run E2E tests
      env:
        TEST_USER_EMAIL: ${{ env.TEST_USER_EMAIL }}
        TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
      run: |
        # Start Y.js multiplayer server
        echo "Starting Y.js multiplayer server..."
        cd multi-player
        MULTIPLAYER_PORT=1234 HOST=0.0.0.0 node server.js &
        MULTIPLAYER_PID=$!
        echo "Multiplayer server started with PID: $MULTIPLAYER_PID"
        sleep 3

        # Start backend
        echo "Starting backend for ${{ matrix.test-type }} E2E tests on Ubuntu (Chrome)..."
        cd ../backend
        uv run uvicorn main:app --host 0.0.0.0 --port 8000 &
        BACKEND_PID=$!
        echo "Backend started with PID: $BACKEND_PID"

        # Wait for backend
        sleep 8
        for i in {1..5}; do
          if curl -f http://localhost:8000/health; then
            echo "Backend ready for E2E tests"
            break
          else
            echo "Waiting for backend... ($i/5)"
            if [ $i -eq 5 ]; then
              echo "Backend failed to start"
              kill $BACKEND_PID 2>/dev/null || true
              kill $MULTIPLAYER_PID 2>/dev/null || true
              exit 1
            fi
            sleep 2
          fi
        done

        # Start frontend
        cd ../frontend
        VITE_MULTIPLAYER_URL=ws://localhost:1234 npm run dev &
        FRONTEND_PID=$!
        echo "Frontend started with PID: $FRONTEND_PID"

        # Wait for frontend
        sleep 15

        # Run tests based on test type (Chrome only)
        if [ "${{ matrix.test-type }}" = "demo" ]; then
          echo "Running demo E2E tests: @core (excluding @mobile-only and @site)"
          TEST_USER_EMAIL="${TEST_USER_EMAIL}" TEST_USER_PASSWORD="${TEST_USER_PASSWORD}" CI_TEST_TYPE=demo npx playwright test --grep "(@core)(?!.*@mobile-only)(?!.*@site)" --project=chromium --reporter=github
        else
          echo "Running auth E2E tests: @auth[^-]|@auth-flows (excluding @mobile-only and @site)"
          TEST_USER_EMAIL="${TEST_USER_EMAIL}" TEST_USER_PASSWORD="${TEST_USER_PASSWORD}" CI_TEST_TYPE=auth npx playwright test --grep "(@auth[^-]|@auth-flows)(?!.*@mobile-only)(?!.*@site)" --project=chromium --reporter=github
        fi
        TEST_EXIT_CODE=$?

        # Cleanup
        echo "Cleaning up processes..."
        kill $FRONTEND_PID 2>/dev/null || true
        kill $BACKEND_PID 2>/dev/null || true
        kill $MULTIPLAYER_PID 2>/dev/null || true
        exit $TEST_EXIT_CODE

  # Desktop E2E Tests (Firefox)
  e2e-desktop-firefox:
    if: false  # Temporarily disabled to speed up CI testing
    # needs: [unit-backend, unit-frontend, unit-site]  # TEMPORARILY DISABLED
    strategy:
      fail-fast: false
      matrix:
        test-type: [demo, auth]
    runs-on: ubuntu-latest
    env:
      DB_URL_LOCAL: postgresql+asyncpg://postgres:postgres@localhost:5432/test_aris
      DB_URL_PROD: postgresql+asyncpg://postgres:postgres@localhost:5432/test_aris
      ALEMBIC_DB_URL_LOCAL: postgresql+psycopg2://postgres:postgres@localhost:5432/test_aris
      ALEMBIC_DB_URL_PROD: postgresql+psycopg2://postgres:postgres@localhost:5432/test_aris
      JWT_SECRET_KEY: test_secret_key_for_ci_environment_32_chars_minimum
      TEST_USER_EMAIL: testuser@aris.pub
      TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
      VITE_LOG_LEVEL: WARN
      VITE_API_BASE_URL: http://localhost:8000
      VITE_MULTIPLAYER_URL: ws://localhost:1234
      COPILOT_PROVIDER: mock

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_aris
          POSTGRES_HOST_AUTH_METHOD: trust
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 3
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Clone RSM repository
      run: |
        cd ..
        git clone --recurse-submodules https://github.com/aris-pub/rsm.git
        echo "Cloned RSM repository to ../rsm"

    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        version: "latest"
        enable-cache: true

    - name: Set up Python
      run: uv python install 3.13

    - name: Wait for PostgreSQL to be ready
      run: |
        timeout 30 bash -c 'until pg_isready -h localhost -p 5432 -U postgres; do sleep 1; done'
        echo "PostgreSQL is ready!"

    - name: Create virtual environment and sync dependencies
      working-directory: backend
      run: |
        uv venv
        uv sync --group test --group dev
        # Force reinstall RSM from local clone to override the locked git version
        uv pip install --reinstall --no-deps -e ../../rsm

    - name: Run database migrations
      working-directory: backend
      run: |
        uv run alembic upgrade head
        ${{ matrix.test-type == 'auth' && 'uv run python scripts/reset_test_user.py' || '' }}

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '23'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm ci

    - name: Install multiplayer server dependencies
      working-directory: multi-player
      run: npm ci

    - name: Cache Playwright browsers
      uses: actions/cache@v5
      id: playwright-cache
      with:
        path: ~/.cache/ms-playwright
        key: playwright-browsers-${{ runner.os }}-firefox

    - name: Install Playwright browsers
      working-directory: frontend
      if: steps.playwright-cache.outputs.cache-hit != 'true'
      run: npx playwright install firefox --with-deps

    - name: Start backend and frontend, run E2E tests
      env:
        TEST_USER_EMAIL: ${{ env.TEST_USER_EMAIL }}
        TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
      run: |
        # Start Y.js multiplayer server
        echo "Starting Y.js multiplayer server..."
        cd multi-player
        MULTIPLAYER_PORT=1234 HOST=0.0.0.0 node server.js &
        MULTIPLAYER_PID=$!
        echo "Multiplayer server started with PID: $MULTIPLAYER_PID"
        sleep 3

        # Start backend
        echo "Starting backend for ${{ matrix.test-type }} E2E tests on Ubuntu (Firefox)..."
        cd ../backend
        uv run uvicorn main:app --host 0.0.0.0 --port 8000 &
        BACKEND_PID=$!
        echo "Backend started with PID: $BACKEND_PID"

        # Wait for backend
        sleep 8
        for i in {1..5}; do
          if curl -f http://localhost:8000/health; then
            echo "Backend ready for E2E tests"
            break
          else
            echo "Waiting for backend... ($i/5)"
            if [ $i -eq 5 ]; then
              echo "Backend failed to start"
              kill $BACKEND_PID 2>/dev/null || true
              kill $MULTIPLAYER_PID 2>/dev/null || true
              exit 1
            fi
            sleep 2
          fi
        done

        # Start frontend
        cd ../frontend
        VITE_MULTIPLAYER_URL=ws://localhost:1234 npm run dev &
        FRONTEND_PID=$!
        echo "Frontend started with PID: $FRONTEND_PID"

        # Wait for frontend
        sleep 15

        # Run tests based on auth mode (Firefox only)
        if [ "${{ matrix.test-type }}" = "demo" ]; then
          echo "Running demo E2E tests: @core (excluding @mobile-only and @site)"
          TEST_USER_EMAIL="${TEST_USER_EMAIL}" TEST_USER_PASSWORD="${TEST_USER_PASSWORD}" CI_TEST_TYPE=demo npx playwright test --grep "(@core)(?!.*@mobile-only)(?!.*@site)" --project=firefox --reporter=github
        else
          echo "Running auth E2E tests: @auth[^-]|@auth-flows (excluding @mobile-only and @site)"
          TEST_USER_EMAIL="${TEST_USER_EMAIL}" TEST_USER_PASSWORD="${TEST_USER_PASSWORD}" CI_TEST_TYPE=auth npx playwright test --grep "(@auth[^-]|@auth-flows)(?!.*@mobile-only)(?!.*@site)" --project=firefox --reporter=github
        fi
        TEST_EXIT_CODE=$?

        # Cleanup
        echo "Cleaning up processes..."
        kill $FRONTEND_PID 2>/dev/null || true
        kill $BACKEND_PID 2>/dev/null || true
        kill $MULTIPLAYER_PID 2>/dev/null || true
        exit $TEST_EXIT_CODE

  # Desktop E2E Tests (Safari)
  e2e-desktop-safari:
    if: false  # Temporarily disabled to speed up CI testing
    # needs: [unit-backend, unit-frontend, unit-site]  # TEMPORARILY DISABLED
    strategy:
      fail-fast: false
      matrix:
        test-type: [demo, auth]
    runs-on: macos-latest
    env:
      TEST_USER_EMAIL: testuser@aris.pub
      TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
      VITE_LOG_LEVEL: WARN
      VITE_MULTIPLAYER_URL: ws://localhost:1234
      COPILOT_PROVIDER: mock

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Clone RSM repository
      run: |
        cd ..
        git clone --recurse-submodules https://github.com/aris-pub/rsm.git
        echo "Cloned RSM repository to ../rsm"

    - name: Install Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'

    - name: Install uv
      run: pip install uv

    - name: Setup PostgreSQL
      run: |
        brew install postgresql
        brew services start postgresql
        # Wait for PostgreSQL to be ready
        for i in {1..30}; do
          if pg_isready -q; then
            echo "PostgreSQL is ready!"
            break
          fi
          if [ $i -eq 30 ]; then
            echo "PostgreSQL failed to start"
            exit 1
          fi
          sleep 2
        done
        # Create postgres user with password to match global DB URLs
        createuser -s postgres
        psql -d postgres -c "ALTER USER postgres PASSWORD 'postgres';"
        createdb test_aris

    - name: Create virtual environment and sync dependencies
      working-directory: backend
      run: |
        uv venv
        uv sync --group test --group dev
        # Force reinstall RSM from local clone to override the locked git version
        uv pip install --reinstall --no-deps -e ../../rsm

    - name: Run database migrations and setup test user (with debugging)
      working-directory: backend
      run: |
        echo "🔍 [DEBUG] Starting database setup for test-type: ${{ matrix.test-type }}"
        echo "🔍 [DEBUG] Environment variables:"
        echo "  - TEST_USER_EMAIL: ${TEST_USER_EMAIL:-UNSET}"
        echo "  - TEST_USER_PASSWORD length: ${#TEST_USER_PASSWORD}"
        echo "  - DB_URL_LOCAL: ${DB_URL_LOCAL:-UNSET}"
        echo "  - PostgreSQL connection test:"

        # Test database connectivity
        if pg_isready -h localhost -p 5432 -U postgres; then
          echo "✅ PostgreSQL connection successful"
        else
          echo "❌ PostgreSQL connection failed"
          exit 1
        fi

        # Run migrations
        echo "🔄 Running database migrations..."
        uv run alembic upgrade head
        MIGRATION_EXIT_CODE=$?
        echo "📊 Migration exit code: $MIGRATION_EXIT_CODE"

        # Setup test user for enabled auth mode
        if [ "${{ matrix.test-type }}" = "auth" ]; then
          echo "🔑 Auth tests - creating test user..."
          echo "📧 Creating test user: ${TEST_USER_EMAIL}"
          uv run python scripts/reset_test_user.py
          RESET_USER_EXIT_CODE=$?
          echo "📊 Reset test user exit code: $RESET_USER_EXIT_CODE"

          if [ $RESET_USER_EXIT_CODE -eq 0 ]; then
            echo "✅ Test user creation/reset completed successfully"
          else
            echo "❌ Test user creation/reset FAILED with exit code: $RESET_USER_EXIT_CODE"
            exit $RESET_USER_EXIT_CODE
          fi
        else
          echo "🔓 Demo tests - skipping test user creation"
        fi

        echo "✅ [DEBUG] Database setup completed for ${{ matrix.test-type }} tests"

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '23'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm ci

    - name: Install multiplayer server dependencies
      working-directory: multi-player
      run: npm ci

    - name: Cache Playwright browsers
      uses: actions/cache@v5
      id: playwright-cache
      with:
        path: ~/Library/Caches/ms-playwright
        key: playwright-browsers-${{ runner.os }}-webkit

    - name: Install Playwright browsers
      working-directory: frontend
      if: steps.playwright-cache.outputs.cache-hit != 'true'
      run: npx playwright install webkit

    - name: Start backend and frontend, run E2E tests
      env:
        TEST_USER_EMAIL: ${{ env.TEST_USER_EMAIL }}
        TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
      run: |
        # Start Y.js multiplayer server
        echo "Starting Y.js multiplayer server..."
        cd multi-player
        MULTIPLAYER_PORT=1234 HOST=0.0.0.0 node server.js &
        MULTIPLAYER_PID=$!
        echo "Multiplayer server started with PID: $MULTIPLAYER_PID"
        sleep 3

        # Start backend
        echo "Starting backend for ${{ matrix.test-type }} E2E tests on macOS (Safari)..."
        cd ../backend
        uv run uvicorn main:app --host 0.0.0.0 --port 8000 &
        BACKEND_PID=$!
        echo "Backend started with PID: $BACKEND_PID"

        # Wait for backend
        sleep 8
        for i in {1..5}; do
          if curl -f http://localhost:8000/health; then
            echo "Backend ready for E2E tests"
            break
          else
            echo "Waiting for backend... ($i/5)"
            if [ $i -eq 5 ]; then
              echo "Backend failed to start"
              kill $BACKEND_PID 2>/dev/null || true
              kill $MULTIPLAYER_PID 2>/dev/null || true
              exit 1
            fi
            sleep 2
          fi
        done

        # Start frontend
        cd ../frontend
        VITE_MULTIPLAYER_URL=ws://localhost:1234 npm run dev &
        FRONTEND_PID=$!
        echo "Frontend started with PID: $FRONTEND_PID"

        # Wait for frontend
        sleep 15

        # Run tests based on auth mode (desktop webkit only)
        if [ "${{ matrix.test-type }}" = "demo" ]; then
          echo "Running demo E2E tests: @core (excluding @mobile-only, @auth, and @site)"
          TEST_USER_EMAIL="${TEST_USER_EMAIL}" TEST_USER_PASSWORD="${TEST_USER_PASSWORD}" CI_TEST_TYPE=demo npx playwright test --grep "(@core)(?!.*@mobile-only)(?!.*@auth)(?!.*@site)" --project=webkit --reporter=github
        else
          echo "Running auth E2E tests: @auth[^-]|@auth-flows (excluding @mobile-only and @site)"
          TEST_USER_EMAIL="${TEST_USER_EMAIL}" TEST_USER_PASSWORD="${TEST_USER_PASSWORD}" CI_TEST_TYPE=auth npx playwright test --grep "(@auth[^-]|@auth-flows)(?!.*@mobile-only)(?!.*@site)" --project=webkit --reporter=github
        fi
        TEST_EXIT_CODE=$?

        # Cleanup
        echo "Cleaning up processes..."
        kill $FRONTEND_PID 2>/dev/null || true
        kill $BACKEND_PID 2>/dev/null || true
        kill $MULTIPLAYER_PID 2>/dev/null || true
        exit $TEST_EXIT_CODE

  # Collaboration E2E Tests (Y.js multi-user)
  e2e-collab:
    if: false  # Temporarily disabled to speed up CI testing
    # needs: [unit-backend, unit-frontend]  # TEMPORARILY DISABLED
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox]
    runs-on: ubuntu-latest
    env:
      TEST_USER_EMAIL: testuser@aris.pub
      TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
      VITE_LOG_LEVEL: WARN
      VITE_MULTIPLAYER_URL: ws://localhost:1234
      COPILOT_PROVIDER: mock

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_aris
          POSTGRES_HOST_AUTH_METHOD: trust
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 3
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Clone RSM repository
      run: |
        cd ..
        git clone --recurse-submodules https://github.com/aris-pub/rsm.git
        echo "Cloned RSM repository to ../rsm"

    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        version: "latest"
        enable-cache: true

    - name: Set up Python
      run: uv python install 3.13

    - name: Wait for PostgreSQL to be ready
      run: |
        timeout 30 bash -c 'until pg_isready -h localhost -p 5432 -U postgres; do sleep 1; done'
        echo "PostgreSQL is ready!"

    - name: Create virtual environment and sync dependencies
      working-directory: backend
      run: |
        uv venv
        uv sync --group test --group dev
        uv pip install -e ../../rsm

    - name: Run database migrations and setup test user
      working-directory: backend
      run: |
        echo "🔍 Setting up database for collaboration tests..."
        uv run alembic upgrade head
        echo "🔑 Creating test user..."
        uv run python scripts/reset_test_user.py
        echo "✅ Database setup completed"

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '23'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm ci

    - name: Install multiplayer server dependencies
      working-directory: multi-player
      run: npm ci

    - name: Cache Playwright browsers
      uses: actions/cache@v5
      id: playwright-cache
      with:
        path: ~/.cache/ms-playwright
        key: playwright-browsers-${{ runner.os }}-${{ matrix.browser }}

    - name: Install Playwright browsers
      working-directory: frontend
      if: steps.playwright-cache.outputs.cache-hit != 'true'
      run: npx playwright install ${{ matrix.browser }} --with-deps

    - name: Start multiplayer, backend, frontend, and run collaboration E2E tests
      env:
        TEST_USER_EMAIL: ${{ env.TEST_USER_EMAIL }}
        TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
      run: |
        # Start Y.js WebSocket server
        echo "Starting Y.js multiplayer server..."
        cd multi-player
        MULTIPLAYER_PORT=1234 HOST=0.0.0.0 node server.js &
        MULTIPLAYER_PID=$!
        echo "Multiplayer server started with PID: $MULTIPLAYER_PID"

        # Wait for multiplayer server (no health endpoint, just wait)
        sleep 3
        echo "Multiplayer server ready"

        # Start backend
        echo "Starting backend for collaboration E2E tests..."
        cd ../backend
        uv run uvicorn main:app --host 0.0.0.0 --port 8000 &
        BACKEND_PID=$!
        echo "Backend started with PID: $BACKEND_PID"

        # Wait for backend
        sleep 8
        for i in {1..5}; do
          if curl -f http://localhost:8000/health; then
            echo "Backend ready for E2E tests"
            break
          else
            echo "Waiting for backend... ($i/5)"
            if [ $i -eq 5 ]; then
              echo "Backend failed to start"
              kill $BACKEND_PID 2>/dev/null || true
              kill $MULTIPLAYER_PID 2>/dev/null || true
              exit 1
            fi
            sleep 2
          fi
        done

        # Start frontend
        cd ../frontend
        VITE_MULTIPLAYER_URL=ws://localhost:1234 npm run dev &
        FRONTEND_PID=$!
        echo "Frontend started with PID: $FRONTEND_PID"

        # Wait for frontend
        sleep 15

        # Install just task runner
        echo "Installing just task runner..."
        curl --proto '=https' --tlsv1.2 -sSf https://just.systems/install.sh | bash -s -- --to /usr/local/bin

        # Run collaboration tests (sequential with --workers=1)
        echo "Running collaboration E2E tests on ${{ matrix.browser }}..."
        cd ../
        just test-collab ${{ matrix.browser }} github
        TEST_EXIT_CODE=$?

        # Cleanup
        echo "Cleaning up processes..."
        kill $FRONTEND_PID 2>/dev/null || true
        kill $BACKEND_PID 2>/dev/null || true
        kill $MULTIPLAYER_PID 2>/dev/null || true
        exit $TEST_EXIT_CODE

  # Mobile E2E Tests (Optional - can fail without breaking CI)
  # TEMPORARILY DISABLED - focusing on desktop E2E fixes
  e2e-mobile-chrome:
    if: false  # Temporarily disabled to speed up CI testing
    # needs: [e2e-desktop-chrome]
    continue-on-error: true  # Allow this job to fail
    strategy:
      fail-fast: false
      matrix:
        test-type: [demo, auth]
    runs-on: ubuntu-latest
    env:
      TEST_USER_EMAIL: testuser@aris.pub
      TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
      VITE_LOG_LEVEL: WARN
      VITE_MULTIPLAYER_URL: ws://localhost:1234
      COPILOT_PROVIDER: mock

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_aris
          POSTGRES_HOST_AUTH_METHOD: trust
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 3
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Clone RSM repository
      run: |
        cd ..
        git clone --recurse-submodules https://github.com/aris-pub/rsm.git
        echo "Cloned RSM repository to ../rsm"

    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        version: "latest"
        enable-cache: true

    - name: Set up Python
      run: uv python install 3.13

    - name: Wait for PostgreSQL to be ready
      run: |
        timeout 30 bash -c 'until pg_isready -h localhost -p 5432 -U postgres; do sleep 1; done'
        echo "PostgreSQL is ready!"

    - name: Create virtual environment and sync dependencies
      working-directory: backend
      run: |
        uv venv
        uv sync --group test --group dev
        uv pip install -e ../../rsm

    - name: Run database migrations and setup test user (with debugging)
      working-directory: backend
      run: |
        echo "🔍 [DEBUG] Starting database setup for test-type: ${{ matrix.test-type }}"
        echo "🔍 [DEBUG] Environment variables:"
        echo "  - TEST_USER_EMAIL: ${TEST_USER_EMAIL:-UNSET}"
        echo "  - TEST_USER_PASSWORD length: ${#TEST_USER_PASSWORD}"
        echo "  - DB_URL_LOCAL: ${DB_URL_LOCAL:-UNSET}"
        echo "  - PostgreSQL connection test:"

        # Test database connectivity
        if pg_isready -h localhost -p 5432 -U postgres; then
          echo "✅ PostgreSQL connection successful"
        else
          echo "❌ PostgreSQL connection failed"
          exit 1
        fi

        # Run migrations
        echo "🔄 Running database migrations..."
        uv run alembic upgrade head
        MIGRATION_EXIT_CODE=$?
        echo "📊 Migration exit code: $MIGRATION_EXIT_CODE"

        # Setup test user for enabled auth mode
        if [ "${{ matrix.test-type }}" = "auth" ]; then
          echo "🔑 Auth tests - creating test user..."
          echo "📧 Creating test user: ${TEST_USER_EMAIL}"
          uv run python scripts/reset_test_user.py
          RESET_USER_EXIT_CODE=$?
          echo "📊 Reset test user exit code: $RESET_USER_EXIT_CODE"

          if [ $RESET_USER_EXIT_CODE -eq 0 ]; then
            echo "✅ Test user creation/reset completed successfully"
          else
            echo "❌ Test user creation/reset FAILED with exit code: $RESET_USER_EXIT_CODE"
            exit $RESET_USER_EXIT_CODE
          fi
        else
          echo "🔓 Demo tests - skipping test user creation"
        fi

        echo "✅ [DEBUG] Database setup completed for ${{ matrix.test-type }} tests"

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '23'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm ci

    - name: Install multiplayer server dependencies
      working-directory: multi-player
      run: npm ci

    - name: Cache Playwright browsers
      uses: actions/cache@v5
      id: playwright-cache
      with:
        path: ~/.cache/ms-playwright
        key: playwright-browsers-${{ runner.os }}-mobile-chrome

    - name: Install Playwright browsers
      working-directory: frontend
      if: steps.playwright-cache.outputs.cache-hit != 'true'
      run: npx playwright install chromium --with-deps

    - name: Platform-specific environment debugging
      run: |
        echo "🔍 [PLATFORM-DEBUG] Platform debugging for Ubuntu:"
        echo "  - Runner OS: $RUNNER_OS"
        echo "  - OS Name: $(uname -s)"
        echo "  - OS Version: $(uname -r)"
        echo "  - Architecture: $(uname -m)"
        echo "  - PostgreSQL version: $(postgres --version 2>/dev/null || echo 'not in PATH')"
        echo "  - Python version: $(python --version 2>/dev/null || echo 'not in PATH')"
        echo "  - Python3 version: $(python3 --version 2>/dev/null || echo 'not in PATH')"
        echo "  - UV version: $(uv --version 2>/dev/null || echo 'not in PATH')"
        echo "  - Available environment variables:"
        env | grep -E "(TEST_|DB_|DISABLE_|VITE_)" | sort || echo "No matching env vars"

    - name: Start backend and frontend, run Mobile Chrome E2E tests
      run: |
        # Start Y.js multiplayer server
        echo "Starting Y.js multiplayer server..."
        cd multi-player
        MULTIPLAYER_PORT=1234 HOST=0.0.0.0 node server.js &
        MULTIPLAYER_PID=$!
        echo "Multiplayer server started with PID: $MULTIPLAYER_PID"
        sleep 3

        # Start backend
        echo "Starting backend for ${{ matrix.test-type }} Mobile Chrome E2E tests..."
        cd ../backend
        uv run uvicorn main:app --host 0.0.0.0 --port 8000 &
        BACKEND_PID=$!
        echo "Backend started with PID: $BACKEND_PID"

        # Wait for backend
        sleep 8
        for i in {1..5}; do
          if curl -f http://localhost:8000/health; then
            echo "Backend ready for E2E tests"
            break
          else
            echo "Waiting for backend... ($i/5)"
            if [ $i -eq 5 ]; then
              echo "Backend failed to start"
              kill $BACKEND_PID 2>/dev/null || true
              kill $MULTIPLAYER_PID 2>/dev/null || true
              exit 1
            fi
            sleep 2
          fi
        done

        # Start frontend
        cd ../frontend
        VITE_MULTIPLAYER_URL=ws://localhost:1234 npm run dev &
        FRONTEND_PID=$!
        echo "Frontend started with PID: $FRONTEND_PID"

        # Wait for frontend
        sleep 15

        # Run mobile Chrome tests
        if [ "${{ matrix.test-type }}" = "demo" ]; then
          echo "Running demo Mobile Chrome E2E tests: @core|@mobile-only (excluding @desktop-only, @auth, and @site)"
          TEST_USER_EMAIL="${TEST_USER_EMAIL}" TEST_USER_PASSWORD="${TEST_USER_PASSWORD}" CI_TEST_TYPE=demo npx playwright test --grep "^(?!.*@auth).*(@core|@mobile-only)(?!.*@desktop-only)(?!.*@site)" --project="Mobile Chrome" --reporter=github
        else
          echo "Running auth Mobile Chrome E2E tests: @auth[^-]|@auth-flows (excluding @desktop-only and @site)"
          TEST_USER_EMAIL="${TEST_USER_EMAIL}" TEST_USER_PASSWORD="${TEST_USER_PASSWORD}" CI_TEST_TYPE=auth npx playwright test --grep "(@auth[^-]|@auth-flows)(?!.*@desktop-only)(?!.*@site)" --project="Mobile Chrome" --reporter=github
        fi
        TEST_EXIT_CODE=$?

        # Cleanup
        echo "Cleaning up processes..."
        kill $FRONTEND_PID 2>/dev/null || true
        kill $BACKEND_PID 2>/dev/null || true
        kill $MULTIPLAYER_PID 2>/dev/null || true
        exit $TEST_EXIT_CODE

  # TEMPORARILY DISABLED - focusing on desktop E2E fixes
  e2e-mobile-firefox:
    if: false  # Temporarily disabled to speed up CI testing
    # needs: [e2e-desktop-firefox]
    continue-on-error: true  # Allow this job to fail
    strategy:
      fail-fast: false
      matrix:
        test-type: [demo, auth]
    runs-on: ubuntu-latest
    env:
      DB_URL_LOCAL: postgresql+asyncpg://postgres:postgres@localhost:5432/test_aris
      DB_URL_PROD: postgresql+asyncpg://postgres:postgres@localhost:5432/test_aris
      ALEMBIC_DB_URL_LOCAL: postgresql+psycopg2://postgres:postgres@localhost:5432/test_aris
      ALEMBIC_DB_URL_PROD: postgresql+psycopg2://postgres:postgres@localhost:5432/test_aris
      JWT_SECRET_KEY: test_secret_key_for_ci_environment_32_chars_minimum
      TEST_USER_EMAIL: testuser@aris.pub
      TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
      VITE_LOG_LEVEL: WARN
      VITE_API_BASE_URL: http://localhost:8000
      VITE_MULTIPLAYER_URL: ws://localhost:1234
      COPILOT_PROVIDER: mock

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_aris
          POSTGRES_HOST_AUTH_METHOD: trust
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 3
        ports:
          - 5432:5432

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Clone RSM repository
      run: |
        cd ..
        git clone --recurse-submodules https://github.com/aris-pub/rsm.git
        echo "Cloned RSM repository to ../rsm"

    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        version: "latest"
        enable-cache: true

    - name: Set up Python
      run: uv python install 3.13

    - name: Wait for PostgreSQL to be ready
      run: |
        timeout 30 bash -c 'until pg_isready -h localhost -p 5432 -U postgres; do sleep 1; done'
        echo "PostgreSQL is ready!"

    - name: Create virtual environment and sync dependencies
      working-directory: backend
      run: |
        uv venv
        uv sync --group test --group dev
        # Force reinstall RSM from local clone to override the locked git version
        uv pip install --reinstall --no-deps -e ../../rsm

    - name: Run database migrations
      working-directory: backend
      run: |
        uv run alembic upgrade head
        ${{ matrix.test-type == 'auth' && 'uv run python scripts/reset_test_user.py' || '' }}

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '23'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm ci

    - name: Install multiplayer server dependencies
      working-directory: multi-player
      run: npm ci

    - name: Cache Playwright browsers
      uses: actions/cache@v5
      id: playwright-cache
      with:
        path: ~/.cache/ms-playwright
        key: playwright-browsers-${{ runner.os }}-mobile-firefox

    - name: Install Playwright browsers
      working-directory: frontend
      if: steps.playwright-cache.outputs.cache-hit != 'true'
      run: npx playwright install firefox --with-deps

    - name: Start backend and frontend, run Mobile Firefox E2E tests
      run: |
        # Start Y.js multiplayer server
        echo "Starting Y.js multiplayer server..."
        cd multi-player
        MULTIPLAYER_PORT=1234 HOST=0.0.0.0 node server.js &
        MULTIPLAYER_PID=$!
        echo "Multiplayer server started with PID: $MULTIPLAYER_PID"
        sleep 3

        # Start backend
        echo "Starting backend for ${{ matrix.test-type }} Mobile Firefox E2E tests..."
        cd ../backend
        uv run uvicorn main:app --host 0.0.0.0 --port 8000 &
        BACKEND_PID=$!
        echo "Backend started with PID: $BACKEND_PID"

        # Wait for backend
        sleep 8
        for i in {1..5}; do
          if curl -f http://localhost:8000/health; then
            echo "Backend ready for E2E tests"
            break
          else
            echo "Waiting for backend... ($i/5)"
            if [ $i -eq 5 ]; then
              echo "Backend failed to start"
              kill $BACKEND_PID 2>/dev/null || true
              kill $MULTIPLAYER_PID 2>/dev/null || true
              exit 1
            fi
            sleep 2
          fi
        done

        # Start frontend
        cd ../frontend
        VITE_MULTIPLAYER_URL=ws://localhost:1234 npm run dev &
        FRONTEND_PID=$!
        echo "Frontend started with PID: $FRONTEND_PID"

        # Wait for frontend
        sleep 15

        # Run mobile Firefox tests
        if [ "${{ matrix.test-type }}" = "demo" ]; then
          echo "Running demo Mobile Firefox E2E tests: @core|@mobile-only (excluding @desktop-only, @auth, and @site)"
          TEST_USER_EMAIL="${TEST_USER_EMAIL}" TEST_USER_PASSWORD="${TEST_USER_PASSWORD}" CI_TEST_TYPE=demo npx playwright test --grep "^(?!.*@auth).*(@core|@mobile-only)(?!.*@desktop-only)(?!.*@site)" --project="Mobile Firefox" --reporter=github
        else
          echo "Running auth Mobile Firefox E2E tests: @auth[^-]|@auth-flows (excluding @desktop-only and @site)"
          TEST_USER_EMAIL="${TEST_USER_EMAIL}" TEST_USER_PASSWORD="${TEST_USER_PASSWORD}" CI_TEST_TYPE=auth npx playwright test --grep "(@auth[^-]|@auth-flows)(?!.*@desktop-only)(?!.*@site)" --project="Mobile Firefox" --reporter=github
        fi
        TEST_EXIT_CODE=$?

        # Cleanup
        echo "Cleaning up processes..."
        kill $FRONTEND_PID 2>/dev/null || true
        kill $BACKEND_PID 2>/dev/null || true
        kill $MULTIPLAYER_PID 2>/dev/null || true
        exit $TEST_EXIT_CODE

  # TEMPORARILY DISABLED - focusing on desktop E2E fixes
  e2e-mobile-safari:
    if: false  # Temporarily disabled to speed up CI testing
    # needs: [e2e-desktop-safari]
    continue-on-error: true  # Allow this job to fail
    strategy:
      fail-fast: false
      matrix:
        test-type: [demo, auth]
    runs-on: macos-latest
    env:
      TEST_USER_EMAIL: testuser@aris.pub
      TEST_USER_PASSWORD: ${{ secrets.TEST_USER_PASSWORD }}
      VITE_LOG_LEVEL: WARN
      COPILOT_PROVIDER: mock

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Clone RSM repository
      run: |
        cd ..
        git clone --recurse-submodules https://github.com/aris-pub/rsm.git
        echo "Cloned RSM repository to ../rsm"

    - name: Install Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'

    - name: Install uv
      run: pip install uv

    - name: Setup PostgreSQL
      run: |
        brew install postgresql
        brew services start postgresql
        # Wait for PostgreSQL to be ready
        for i in {1..30}; do
          if pg_isready -q; then
            echo "PostgreSQL is ready!"
            break
          fi
          if [ $i -eq 30 ]; then
            echo "PostgreSQL failed to start"
            exit 1
          fi
          sleep 2
        done
        # Create postgres user with password to match global DB URLs
        createuser -s postgres
        psql -d postgres -c "ALTER USER postgres PASSWORD 'postgres';"
        createdb test_aris

    - name: Create virtual environment and sync dependencies
      working-directory: backend
      run: |
        uv venv
        uv sync --group test --group dev
        # Force reinstall RSM from local clone to override the locked git version
        uv pip install --reinstall --no-deps -e ../../rsm

    - name: Run database migrations and setup test user (with debugging)
      working-directory: backend
      run: |
        echo "🔍 [DEBUG] Starting database setup for test-type: ${{ matrix.test-type }}"
        echo "🔍 [DEBUG] Environment variables:"
        echo "  - TEST_USER_EMAIL: ${TEST_USER_EMAIL:-UNSET}"
        echo "  - TEST_USER_PASSWORD length: ${#TEST_USER_PASSWORD}"
        echo "  - DB_URL_LOCAL: ${DB_URL_LOCAL:-UNSET}"
        echo "  - PostgreSQL connection test:"

        # Test database connectivity
        if pg_isready -q; then
          echo "✅ PostgreSQL connection successful"
        else
          echo "❌ PostgreSQL connection failed"
          exit 1
        fi

        # Run migrations
        echo "🔄 Running database migrations..."
        uv run alembic upgrade head
        MIGRATION_EXIT_CODE=$?
        echo "📊 Migration exit code: $MIGRATION_EXIT_CODE"

        # Setup test user for enabled auth mode
        if [ "${{ matrix.test-type }}" = "auth" ]; then
          echo "🔑 Auth tests - creating test user..."
          echo "📧 Creating test user: ${TEST_USER_EMAIL}"
          uv run python scripts/reset_test_user.py
          RESET_USER_EXIT_CODE=$?
          echo "📊 Reset test user exit code: $RESET_USER_EXIT_CODE"

          if [ $RESET_USER_EXIT_CODE -eq 0 ]; then
            echo "✅ Test user creation/reset completed successfully"
          else
            echo "❌ Test user creation/reset FAILED with exit code: $RESET_USER_EXIT_CODE"
            exit $RESET_USER_EXIT_CODE
          fi
        else
          echo "🔓 Demo tests - skipping test user creation"
        fi

        echo "✅ [DEBUG] Database setup completed for ${{ matrix.test-type }} tests"

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '23'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm ci

    - name: Install multiplayer server dependencies
      working-directory: multi-player
      run: npm ci

    - name: Cache Playwright browsers
      uses: actions/cache@v5
      id: playwright-cache
      with:
        path: ~/Library/Caches/ms-playwright
        key: playwright-browsers-${{ runner.os }}-mobile-safari

    - name: Install Playwright browsers
      working-directory: frontend
      if: steps.playwright-cache.outputs.cache-hit != 'true'
      run: npx playwright install webkit

    - name: Platform-specific environment debugging
      run: |
        echo "🔍 [PLATFORM-DEBUG] Platform debugging for macOS:"
        echo "  - Runner OS: $RUNNER_OS"
        echo "  - OS Name: $(uname -s)"
        echo "  - OS Version: $(uname -r)"
        echo "  - Architecture: $(uname -m)"
        echo "  - PostgreSQL version: $(postgres --version 2>/dev/null || echo 'not in PATH')"
        echo "  - Python version: $(python --version 2>/dev/null || echo 'not in PATH')"
        echo "  - Python3 version: $(python3 --version 2>/dev/null || echo 'not in PATH')"
        echo "  - UV version: $(uv --version 2>/dev/null || echo 'not in PATH')"
        echo "  - Brew info: $(brew --version 2>/dev/null || echo 'not in PATH')"
        echo "  - Available environment variables:"
        env | grep -E "(TEST_|DB_|DISABLE_|VITE_)" | sort || echo "No matching env vars"

    - name: Start backend and frontend, run Mobile Safari E2E tests
      run: |
        # Start Y.js multiplayer server
        echo "Starting Y.js multiplayer server..."
        cd multi-player
        MULTIPLAYER_PORT=1234 HOST=0.0.0.0 node server.js &
        MULTIPLAYER_PID=$!
        echo "Multiplayer server started with PID: $MULTIPLAYER_PID"
        sleep 3

        # Start backend
        echo "Starting backend for ${{ matrix.test-type }} Mobile Safari E2E tests..."
        cd ../backend
        uv run uvicorn main:app --host 0.0.0.0 --port 8000 &
        BACKEND_PID=$!
        echo "Backend started with PID: $BACKEND_PID"

        # Wait for backend
        sleep 8
        for i in {1..5}; do
          if curl -f http://localhost:8000/health; then
            echo "Backend ready for E2E tests"
            break
          else
            echo "Waiting for backend... ($i/5)"
            if [ $i -eq 5 ]; then
              echo "Backend failed to start"
              kill $BACKEND_PID 2>/dev/null || true
              kill $MULTIPLAYER_PID 2>/dev/null || true
              exit 1
            fi
            sleep 2
          fi
        done

        # Start frontend
        cd ../frontend
        VITE_MULTIPLAYER_URL=ws://localhost:1234 npm run dev &
        FRONTEND_PID=$!
        echo "Frontend started with PID: $FRONTEND_PID"

        # Wait for frontend
        sleep 15

        # Run mobile Safari tests
        if [ "${{ matrix.test-type }}" = "demo" ]; then
          echo "Running demo Mobile Safari E2E tests: @core|@mobile-only (excluding @desktop-only, @auth, and @site)"
          TEST_USER_EMAIL="${TEST_USER_EMAIL}" TEST_USER_PASSWORD="${TEST_USER_PASSWORD}" CI_TEST_TYPE=demo npx playwright test --grep "^(?!.*@auth).*(@core|@mobile-only)(?!.*@desktop-only)(?!.*@site)" --project="Mobile Safari" --reporter=github
        else
          echo "Running auth Mobile Safari E2E tests: @auth[^-]|@auth-flows (excluding @desktop-only and @site)"
          TEST_USER_EMAIL="${TEST_USER_EMAIL}" TEST_USER_PASSWORD="${TEST_USER_PASSWORD}" CI_TEST_TYPE=auth npx playwright test --grep "(@auth[^-]|@auth-flows)(?!.*@desktop-only)(?!.*@site)" --project="Mobile Safari" --reporter=github
        fi
        TEST_EXIT_CODE=$?

        # Cleanup
        echo "Cleaning up processes..."
        kill $FRONTEND_PID 2>/dev/null || true
        kill $BACKEND_PID 2>/dev/null || true
        kill $MULTIPLAYER_PID 2>/dev/null || true
        exit $TEST_EXIT_CODE


# Trigger CI

  # EXPERIMENTAL: Run collab tests with Docker Compose (test environment hypothesis)
  e2e-collab-docker:
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium]  # Start with chromium only
    runs-on: ubuntu-latest
    env:
      # CI-specific overrides (everything else comes from .env.example)
      ENV: CI
      VITE_LOG_LEVEL: WARN
      COMPOSE_FILE: docker/docker-compose.dev.yml
      COMPOSE_PROJECT_NAME: studio-e2e-collab

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Clone RSM repository
      run: |
        cd ..
        git clone --recurse-submodules https://github.com/aris-pub/rsm.git
        echo "Cloned RSM repository to ../rsm"

    - name: Setup Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '23'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm ci

    - name: Cache Playwright browsers
      uses: actions/cache@v5
      id: playwright-cache
      with:
        path: ~/.cache/ms-playwright
        key: playwright-browsers-${{ runner.os }}-${{ matrix.browser }}

    - name: Install Playwright browsers
      working-directory: frontend
      if: steps.playwright-cache.outputs.cache-hit != 'true'
      run: npx playwright install ${{ matrix.browser }} --with-deps

    - name: Create .env file
      run: cp .env.example .env

    - name: Create backend .env file
      run: cp backend/.env.example backend/.env

    - name: Debug - Gather evidence about .env file
      run: |
        echo "📍 Current directory: $(pwd)"
        echo ""
        echo "📄 .env file exists?"
        ls -la .env
        echo ""
        echo "📄 .env file contents:"
        cat .env
        echo ""
        echo "📄 backend/.env file exists?"
        ls -la backend/.env
        echo ""
        echo "📄 Docker Compose version:"
        docker compose version
        echo ""
        echo "📄 start-dev.sh location:"
        ls -la scripts/start-dev.sh
        echo ""
        echo "📄 docker-compose.dev.yml env_file directive:"
        grep -A2 "env_file:" docker/docker-compose.dev.yml || echo "No env_file directive found"

    - name: Clean up Docker volumes
      run: |
        echo "🧹 Cleaning up any existing Docker volumes..."
        docker compose --env-file .env down -v 2>/dev/null || true
        docker volume prune -f || true
        echo "✅ Docker cleanup complete"

    - name: Start Docker Compose services
      run: |
        echo "🐳 Starting services using start-dev.sh (same as local dev)..."
        ./scripts/start-dev.sh

        echo "⏳ Waiting for services to be ready (30s)..."
        sleep 30

        echo "📊 Service status:"
        docker compose --env-file .env ps

        echo "🔍 Checking service health..."
        curl -f http://localhost:8000/health || echo "⚠️  Backend health check failed"
        curl -f http://localhost:5173 || echo "⚠️  Frontend health check failed"

        echo "✅ Docker services ready"

    - name: Setup database and test user in Docker
      run: |
        echo "🔄 Running database migrations in Docker..."
        docker compose --env-file .env exec -T backend uv run alembic upgrade head

        echo "👤 Creating test user in Docker..."
        docker compose --env-file .env exec -T backend uv run python scripts/reset_test_user.py

        echo "✅ Database setup complete"

    - name: Run single-tab Y.js E2E tests
      working-directory: frontend
      run: |
        echo "🧪 Running single-tab Y.js tests on ${{ matrix.browser }} (Docker environment)..."
        npx playwright test yjs-single-tab.spec.js --project=${{ matrix.browser }} --reporter=github --workers=1

    - name: Upload test results on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: playwright-results-docker-${{ matrix.browser }}
        path: frontend/test-results/
        retention-days: 7

    - name: Show Docker logs on failure
      if: failure()
      run: |
        echo "📋 Backend logs:"
        docker compose --env-file .env logs backend
        echo "📋 Frontend logs:"
        docker compose --env-file .env logs frontend
        echo "📋 Multiplayer logs:"
        docker compose --env-file .env logs collab

    - name: Cleanup Docker services
      if: always()
      run: |
        echo "🧹 Cleaning up Docker services..."
        docker compose --env-file .env down -v
        echo "✅ Cleanup complete"

