[build]
  publish = ".output/public"
  command = "cd .. && pnpm install --frozen-lockfile && pnpm --filter site run generate"

[build.environment]
  PNPM_VERSION = "9"

[dev]
  command = "pnpm run dev"
  port = "${SITE_PORT}"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Custom domain configuration
# Add rsm.studio in Netlify dashboard: Site settings > Domain management
