# Design System (Deprecated)

**This directory is deprecated.** The actual styles are served from `styles/css/`.

See `styles/README.md` for the current documentation and usage.

## Migration Note

This directory contains duplicate copies of CSS files that were historically used but are no longer served by the backend. The canonical CSS files are in `styles/css/` and are served at `/styles/css/` endpoint.
